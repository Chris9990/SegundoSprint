@model IEnumerable<Gestion.Models.Proveedores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Codigo Proveedor
        </th>
        <th>
            Empresa
        </th>
        <th>
            Nombre
        </th>
        <th>
            Apellido
        </th>
        <th>
            Teléfono
        </th>
        <th>
            Dirección
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_Prov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Prov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellido_Prov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono_Prov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion_Prov)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Cod_Prov }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Cod_Prov  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Cod_Prov  })
        </td>
    </tr>
}

</table>
