@model IEnumerable<Gestion.Models.Metodo_de_Pago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Código MétodoPago
        </th>
        <th>
            Método
        </th>
        <th>
            Descripción
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_MPago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Metodo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Cod_MPago  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Cod_MPago }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Cod_MPago  })
        </td>
    </tr>
}

</table>
