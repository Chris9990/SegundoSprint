@model IEnumerable<Gestion.Models.Clientes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Código Cliente
        </th>
        <th>
            Nombre
        </th>
        <th>
            Apellido
        </th>
        <th>
            Dirección
        </th>
        <th>
            Teléfono
        </th>
        <th>
            Código Metodo de Pago
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellido_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_MPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Cod_Cliente  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Cod_Cliente  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Cod_Cliente  })
        </td>
    </tr>
}

</table>
