@model IEnumerable<Gestion.Models.Repartidores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Codigo Repartidor
        </th>
        <th>
            CI
        </th>
        <th>
            Teléfono
        </th>
        <th>
            Nombre
        </th>
        <th>
            Placa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_Repartidor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono_Repartidor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Repartidor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Placa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Cod_Repartidor  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Cod_Repartidor  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Cod_Repartidor  })
        </td>
    </tr>
}

</table>
