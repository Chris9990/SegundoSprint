@model IEnumerable<Gestion.Models.Repartidores>

@{
    ViewBag.Title = "Index";
}

<h2>Repartidores</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefono_Repartidor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Repartidor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Placa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono_Repartidor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Repartidor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Placa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cod_Repartidores }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cod_Repartidores }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cod_Repartidores })
        </td>
    </tr>
}

</table>
